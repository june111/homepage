import React from 'react';
import {copyright} from '../store/db'

const element =
    <section>
		<div>{copyright}</div>
	</section>

export const Footer = () => {
    return (
        element
    )
};