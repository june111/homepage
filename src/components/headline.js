import React from 'react';

const name = 'June Zhu';
const element =
    <section>
		<div>{name}</div>
		<button>blog</button>
	</section>

export const Headline = () => {
    return (
        element
    )
};