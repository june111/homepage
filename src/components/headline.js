import React from 'react';

const name = 'June Zhu';
const element =
    <section className="big">
		<div>{name}</div>
		<button>blog</button>
	</section>

export const Headline = () => {
    return (
        element
    )
};